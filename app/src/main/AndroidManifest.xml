<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cards.cah" >

    <application
        android:name=".CardsApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameHomeActivity"
            android:label="@string/title_activity_game_home"
            android:launchMode="singleTask"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.cards.cah.MainActivity" />
        </activity>
        <activity
            android:name=".CardSelectActivity"
            android:label="@string/title_activity_card_select"
            android:launchMode="singleTop"
            android:parentActivityName=".GameHomeActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.cards.cah.GameHomeActivity" />
        </activity>
        <activity
            android:name=".SelectedAnswerActivity"
            android:label="@string/title_activity_selected_answer"
            android:parentActivityName=".GameHomeActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.cards.cah.GameHomeActivity" />
        </activity>
    </application>

</manifest>
